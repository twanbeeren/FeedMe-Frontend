import { FilterByCoursePipe } from './filter-by-course.pipe';

describe('FilterByCoursePipe', () => {
  it('create an instance', () => {
    const pipe = new FilterByCoursePipe();
    expect(pipe).toBeTruthy();
  });
});
