import { SortCoursesPipe } from './sort-courses.pipe';

describe('SortCoursesPipe', () => {
  it('create an instance', () => {
    const pipe = new SortCoursesPipe();
    expect(pipe).toBeTruthy();
  });
});
